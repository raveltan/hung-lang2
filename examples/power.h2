var power = f(a,b){
    var calculatePower = f(value,iteration,end,target){
        if(iteration != end){
        return calculatePower(value * target, iteration +1, end,target)
        }
        value
    }
    calculatePower(1,0,b,a)
}
var powOne = power(5,3)

var newPowerFunction = f(a,b){
    var res = 1;
    for(1,b,1, f(){
        var res = res * a;
    })
    return res;
}

var powTwo = newPowerFunction(5,3)
write(powOne,powTwo);