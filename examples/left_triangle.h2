var leftTriangle = f(i){
    var pref = '*'
    var temp = ''
    for(1,i,1,f(){
       var temp = temp + pref
       write(temp)
    })
}

write('Enter amount of rows:' )
var input = read()
leftTriangle(num(input))